This folder is used for mission cache inspection in the Unity Editor.

Runtime cache file is written to:
Application.persistentDataPath/missions_cache.json

In the Editor, the latest fetched missions may also be exported to:
Assets/Resources/Missions/missions_cache.json
